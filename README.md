# Smart City Актау

Платформа гражданского участия для города Актау с интеграцией карт, системой обратной связи, аналитикой трафика и push-уведомлениями.

## Функционал

### Основные возможности
- Интерактивная карта — TomTom Maps с маркерами трафика, POI, и микрорайонов
- Система жалоб — отправка жалоб с фото (до 3 шт), статусы, фильтры, детальный просмотр
- Админ-панель — управление жалобами (смена статуса, комментарий, push-уведомления) и новостями (создание/редактирование/публикация)
- Дашборд — трафик, погода (температура, ветер, влажность, давление), транспортная загруженность, очаги пробок
- AI-рекомендации — персонализированные советы по погоде, трафику, транспорту на RU/KZ (кнопка с градиентом и иконкой Sparkles)
- Аналитика — микрорайоны по загруженности, спарклайн динамики трафика
- Динамические новости — генерация на основе данных трафика + админские новости
- PWA — Progressive Web App с офлайн-кэшем и push-уведомлениями
- Мультиязычность — RU/KZ с мгновенным переключением на всех страницах
- Профиль — редактирование данных, настройки, статистика

### Технологические особенности
- Билингвальный интерфейс (RU/KZ)
- LocalStorage-кэш для офлайн-работы
- Web Push Notifications (автору жалобы и админу)
- Адаптивная верстка (десктоп + мобайл)
- Framer Motion анимации

## Технологический стек

### Frontend
- Next.js 16 (App Router, React Server Components)
- TypeScript
- Tailwind CSS v4 — стилизация
- Framer Motion — анимации
- Lucide Icons — монохромные иконки
- Radix UI — компоненты (Sidebar, Sheet, Dialog, Toast)

### Backend & Services
- Supabase — аутентификация, БД (PostgreSQL), Storage для фото
- TomTom Traffic Flow API — данные о трафике
- Overpass API (OpenStreetMap) — границы микрорайонов
- OpenWeather API — погода (температура, ветер, влажность, давление)
- 2GIS Routes API — расчет задержек транспорта
- Cerebras AI — персонализированные рекомендации на RU/KZ с учетом погоды, трафика, транспорта
- Web Push (VAPID) — push-уведомления

### Maps & Data
- Leaflet — интерактивные карты
- OpenStreetMap — тайлы карт

## Установка

### 1. Клонировать репозиторий
```bash
git clone https://github.com/qaddasd/smart-city-aktau.git
cd smart-city-aktau
```

### 2. Установить зависимости
```bash
npm install
```

### 3. Настроить переменные окружения
Создайте `.env.local`:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# TomTom API 
TOMTOM_API_KEY=your_tomtom_api_key

# OpenWeather API
NEXT_PUBLIC_OPENWEATHER_API_KEY=your_openweather_api_key

# VAPID keys для push-уведомлений 
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key

# Supabase Service Role (для админских операций)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Cerebras AI (персонализированные рекомендации)
CEREBRAS_API_KEY=csk-your_cerebras_api_key
```

### 4. Сгенерировать VAPID ключи
```bash
npx web-push generate-vapid-keys
```
Скопируйте ключи в `.env.local`.

### 5. Настроить БД в Supabase
1. Создайте проект на [supabase.com](https://supabase.com)
2. Перейдите в **SQL Editor**
3. Скопируйте и выполните содержимое файла `supabase-migrations.sql`

Миграция создаст:
- Колонки для push-подписок (`push_endpoint`, `push_p256dh`, `push_auth`)
- Колонку `admin_comment` для комментариев админа
- Колонку `photos` (jsonb) для массива URL фотографий
- Bucket `feedback-photos` в Supabase Storage
- Политики доступа к Storage
- Индексы для оптимизации запросов
- AI-колонки для будущей интеграции ML (категория, приоритет, дубликаты, теги фото)

### 6. Запуск в dev-режиме
```bash
npm run dev
```
Откройте [http://localhost:3000](http://localhost:3000)

### 7. Production build
```bash
npm run build
npm start
```

## API ключи

### TomTom Maps API
1. Зарегистрируйтесь на [developer.tomtom.com](https://developer.tomtom.com)
2. Создайте API ключ
3. Включите продукт **Traffic Flow**
4. Разрешите домен `http://localhost` (dev) и продакшн-домен
5. Добавьте ключ в `TOMTOM_API_KEY`

### OpenWeather API
1. Зарегистрируйтесь на [openweathermap.org/api](https://openweathermap.org/api)
2. Создайте бесплатный API ключ
3. Добавьте в `NEXT_PUBLIC_OPENWEATHER_API_KEY`

### Supabase
1. Создайте проект на [supabase.com](https://supabase.com)
2. В **Project Settings → API** найдите:
   - `URL` → `NEXT_PUBLIC_SUPABASE_URL`
   - `anon public` → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `service_role` → `SUPABASE_SERVICE_ROLE_KEY` (для админских операций)

### Cerebras AI
1. Зарегистрируйтесь на [cerebras.ai](https://cerebras.ai)
2. Создайте API ключ
3. Добавьте в `CEREBRAS_API_KEY`
4. Используется модель `llama-3.3-70b` для генерации персонализированных рекомендаций на RU/KZ

## Структура проекта

```
app/
├── api/
│   ├── traffic/
│   │   ├── summary/route.ts      # Сводка трафика + топ-очаги
│   │   └── areas/route.ts        # Микрорайоны с загруженностью
│   ├── push/
│   │   └── subscribe/route.ts    # Подписка на push
│   └── feedback/
│       ├── admin/update/route.ts # Админ: обновление статуса + push
│       └── notify-admin/route.ts # Уведомление админа о новой жалобе
├── dashboard/page.tsx            # Дашборд (трафик, погода, транспорт, районы)
├── analytics/page.tsx            # Аналитика (районы, спарклайн)
├── news/page.tsx                 # Динамические новости + поиск
├── submissions/
│   ├── page.tsx                  # Список жалоб с фильтрами
│   └── [id]/page.tsx             # Детальный просмотр + удаление
├── admin/feedback/page.tsx       # Админ-панель управления жалобами
├── profile/
│   ├── page.tsx                  # Профиль
│   ├── edit/page.tsx             # Редактирование профиля
│   └── settings/page.tsx         # Настройки (язык, push)
├── map/page.tsx                  # Карта
├── login/page.tsx                # Вход
├── register/page.tsx             # Регистрация
└── layout.tsx                    # Root layout (PWA manifest, service worker)

components/
├── ui/                           # Radix UI компоненты
├── app-layout.tsx                # Десктоп/мобайл layout, sidebar, язык
├── mobile-nav.tsx                # Нижняя навигация для мобайл
├── report-form.tsx               # Форма отправки жалобы (+ фото)
└── pwa-client.tsx                # Регистрация service worker

lib/
├── supabase-client.ts            # Браузерный клиент Supabase
├── supabase-server.ts            # Серверный клиент Supabase
├── 2gis.ts                       # 2GIS API для задержек транспорта
├── weather.ts                    # OpenWeather API
├── tomtom.ts                     # TomTom Traffic Flow
├── osm.ts                        # Overpass API для микрорайонов
├── pwa.ts                        # Push subscription helpers
└── toast.ts                      # Toast notifications

public/
├── logo.png                      # Логотип приложения
├── manifest.json                 # PWA манифест
└── sw.js                         # Service Worker (офлайн-кэш, push)

supabase-migrations.sql           # SQL миграции для БД
```

## База данных (Supabase)

### Таблицы

#### `users`
- `id` (uuid, PK)
- `email` (text)
- `full_name` (text)
- `phone` (text)
- `push_endpoint` (text) — Web Push endpoint
- `push_p256dh` (text) — Push ключ p256dh
- `push_auth` (text) — Push ключ auth
- `created_at` (timestamp)
- `updated_at` (timestamp)

#### `feedback`
- `id` (uuid, PK)
- `user_id` (uuid, FK → users)
- `case_id` (text) — ID жалобы (C-XXXXX)
- `title` (text)
- `description` (text)
- `category` (text) — roads/safety/parks/utilities/other
- `status` (text) — Received/In Progress/Resolved/Rejected
- `admin_comment` (text) — Комментарий админа
- `photos` (jsonb) — Массив URL фотографий
- `ai_category` (text) — AI-детектированная категория
- `ai_priority` (text) — AI-приоритет
- `ai_summary` (text) — AI-резюме
- `ai_duplicate_of` (uuid) — Ссылка на дубликат
- `photo_ai_tags` (jsonb) — AI-теги с фото
- `created_at` (timestamp)
- `updated_at` (timestamp)

### Storage Bucket
- **feedback-photos** — публичное хранилище фотографий жалоб

## Админ-доступ

**Email:** `ch.qynon@gmail.com`  
**Админ может:**

### Управление жалобами (`/admin/feedback`)
- Просматривать все жалобы
- Фильтровать по статусам (Все/Получено/В процессе/Решено/Отклонено)
- Менять статус (Received → In Progress → Resolved/Rejected)
- Добавлять комментарии
- При сохранении автор жалобы получает push-уведомление
- При новой жалобе админ получает push-уведомление

### Управление новостями (`/admin/news`)
- Создавать новости (билингвальные RU/KZ)
- Редактировать существующие новости
- Удалять новости
- Опубликовать/снять с публикации
- Устанавливать категорию (Дорожная обстановка/События/Уведомления/Инфраструктура)
- Устанавливать приоритет (Высокий/Средний/Низкий)
- Добавлять локацию и изображение (опционально)
- Новости отображаются на странице `/news` вместе с автогенерированными

## Загрузка фото

**Почему сейчас нельзя вложить фото:**
- Кнопка прикрепления есть в форме, но обработчик загрузки не реализован
- Нужно добавить:
  1. Input type=file (multiple, до 3 файлов)
  2. Загрузку в Supabase Storage bucket `feedback-photos`
  3. Сохранение URL массива в колонку `photos` (jsonb)
  4. Отображение миниатюр в детальной странице жалобы

**Реализация (следующий шаг):**
```tsx
// В components/report-form.tsx
const [photos, setPhotos] = useState<File[]>([])

const uploadPhotos = async (feedbackId: string) => {
  const urls: string[] = []
  for (const file of photos) {
    const path = `${user.id}/${feedbackId}/${file.name}`
    await supabase.storage.from('feedback-photos').upload(path, file)
    const { data } = supabase.storage.from('feedback-photos').getPublicUrl(path)
    urls.push(data.publicUrl)
  }
  await supabase.from('feedback').update({ photos: urls }).eq('id', feedbackId)
}
```

## AI интеграция (roadmap)

### 1. **Автотриаж жалоб** (классификация)
- **Модель:** DistilBERT / XGBoost на описании жалобы
- **Что делает:** автоматически определяет категорию (roads/safety/parks/utilities) и приоритет (high/medium/low)
- **Колонка:** `ai_category`, `ai_priority`
- **Интеграция:** API endpoint `/api/ai/classify` вызывается после отправки жалобы

### 2. **Детекция дубликатов** (NLP embeddings)
- **Модель:** Sentence Transformers (multilingual) + косинусное сходство
- **Что делает:** находит похожие жалобы по тексту и геолокации
- **Колонка:** `ai_duplicate_of` (uuid)
- **UI:** при отправке показывает "Похожие жалобы уже есть, посмотреть?"

### 3. **Анализ фото** (Computer Vision)
- **Модель:** YOLOv8 / CLIP / Google Vision API
- **Что делает:**
  - Детектирует объекты (яма, мусор, граффити, разбитый фонарь)
  - Извлекает текст с вывесок/номеров (OCR)
  - Оценивает качество фото (блюр, освещение)
- **Колонка:** `photo_ai_tags` (jsonb)
- **Пример:** `["pothole", "road_damage", "low_light"]`

### 4. **Автосводка новостей** (LLM)
- **Модель:** GPT-4 / Claude / Llama 2
- **Что делает:** генерирует ежедневный дайджест новостей из трафика и жалоб на 2 языках
- **UI:** блок "Город за сегодня" на главной

### 5. **Умные рекомендации пользователю** (Recommender System)
- **Что делает:**
  - Предсказывает время пробок на привычных маршрутах
  - Рекомендует время выезда
  - Предупреждает о проблемных участках
- **Колонка:** таблица `user_routes` (сохраняет привычки)

### 6. **Автоответ админа** (LLM RAG)
- **Модель:** GPT-4 + база знаний (FAQ, регламенты)
- **Что делает:** предлагает админу шаблон ответа на основе категории жалобы и истории решений
- **UI:** кнопка "Сгенерировать ответ" в админ-панели

### 7. **Мультиязычный перевод** (NMT)
- **Модель:** mBART / NLLB / Google Translate API
- **Что делает:** автоперевод жалоб RU ↔ KZ для удобства админа

### 8. **Голосовая диктовка жалобы** (ASR)
- **Модель:** Whisper / Google Speech-to-Text
- **Что делает:** пользователь диктует жалобу голосом, текст появляется в форме
- **UI:** кнопка микрофона в форме жалобы

## PWA (Progressive Web App)

### Установка на устройство
- **Десктоп (Chrome/Edge):** кнопка "Установить" в адресной строке
- **Android:** "Добавить на главный экран" в меню браузера
- **iOS:** "Поделиться" → "На экран Домой"

### Возможности
- Работа офлайн (кэш статики и API-запросов)
- Push-уведомления (статус жалобы, новости)
- Иконка на главном экране
- Запуск в полноэкранном режиме

### Service Worker (`public/sw.js`)
- Кэширует статику (JS, CSS, HTML)
- Кэширует API-ответы (traffic, areas, weather)
- Обрабатывает push-уведомления
- Открывает нужную страницу при клике на уведомление

## Деплой

### Vercel (рекомендуется)
```bash
vercel
```
Добавьте все переменные окружения в Vercel Dashboard.

### Другие платформы
- **Netlify:** аналогично Vercel
- **Railway:** поддерживает Next.js
- **Render:** настройте build command `npm run build`, start `npm start`

## Тестирование

### Сценарии для проверки
1. **Регистрация/Логин**
2. **Отправка жалобы** (с фото, после реализации)
3. **Фильтрация жалоб** по статусам
4. **Детальный просмотр** и удаление жалобы
5. **Админ-панель** (`ch.qynon@gmail.com`):
   - Смена статуса → проверить push автору
   - Комментарий → сохранить
6. **Push-уведомления**:
   - Включить в "Настройки"
   - Отправить жалобу → админ должен получить push
   - Админ меняет статус → автор получает push
7. **Дашборд**:
   - Данные трафика, погода, транспорт
   - Спарклайн обновляется каждую минуту
8. **Новости**:
   - Динамические новости от трафика
   - Поиск работает
9. **Смена языка**: RU ↔ KZ мгновенно на всех страницах
10. **PWA**: установить и работать офлайн

## Контрибьюция

1. Fork проекта
2. Создайте ветку: `git checkout -b feature/new-feature`
3. Коммит: `git commit -m 'Add new feature'`
4. Push: `git push origin feature/new-feature`
5. Откройте Pull Request

## Лицензия

MIT License

## Контакты

Админ: ch.qynon@gmail.com

---

**Smart City Актау** — делаем город удобнее вместе! 🏙✨
